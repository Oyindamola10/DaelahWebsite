import React from "react";

const ShoppingCart = () => {
  
  return (
    <div>
      
      {/* <h4>View your Cart</h4>
      <ul>
        {cartItems.map((item) => (
          <li key={item.id}>
            {item.name} - NGN{item.price}
          </li>
        ))}
      </ul> */}
    </div>
  );
};

export default ShoppingCart;
